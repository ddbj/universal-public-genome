[config]
gz_file = /path/to/gene2accession.gz
db_file = /path/to/gene2accession.db
working_dir = /tmp/
ncbi_dataset_zip_file = ncbi_dataset.zip

# No configuration is needed if 'datasets' is already in your system PATH.
datasets_tool_path = /path/to/ncbi/tool/datasets